{
  "name": "nagoya-tsunami-3d-color-relief",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "check:endpoints": "node scripts/check-endpoints.mjs",
    "inspect:pmtiles": "node scripts/inspect-pmtiles.mjs",
    "make:icons": "node scripts/make-icons.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^6.6.0",
    "pmtiles": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
